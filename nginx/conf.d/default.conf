# ---------------------------------------------------------------------------
# 이 파일은 Docker에서 사용되지 않습니다.
#
# 실제 nginx 설정은 nginx/templates/default.conf.template 을 수정하세요.
# Docker Compose는 nginx/templates/ 만 마운트하며, envsubst를 통해
# /etc/nginx/conf.d/default.conf 로 자동 생성됩니다.
#
# docker-compose.yml volumes:
#   - ./nginx/templates:/etc/nginx/templates:ro  ← 실제 사용
#   (nginx/conf.d/ 는 마운트되지 않음)
# ---------------------------------------------------------------------------
