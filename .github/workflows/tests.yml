name: Test Suite

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  api-tests:
    name: API Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python 3.13
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'

      - name: Install dependencies
        working-directory: itsm-api
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov httpx

      - name: Run tests
        working-directory: itsm-api
        env:
          DATABASE_URL: sqlite:///./test.db
          GITLAB_API_URL: http://fake-gitlab
          GITLAB_ADMIN_TOKEN: test-token
          GITLAB_PROJECT_ID: "1"
          ENVIRONMENT: development
          CORS_ORIGINS: http://localhost
          SECRET_KEY: test-secret-key-at-least-32-chars-long
          REDIS_URL: redis://localhost:6379
        run: |
          pytest tests/ -v \
            --cov=app \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=78 \
            --tb=short

      - name: Upload coverage
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: itsm-api/coverage.xml

  web-checks:
    name: Web Typecheck & Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node 20
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: itsm-web/package-lock.json

      - name: Install dependencies
        working-directory: itsm-web
        run: npm ci

      - name: Typecheck (tsc --noEmit, strict)
        working-directory: itsm-web
        run: npm run typecheck

      - name: Unit tests (jest)
        working-directory: itsm-web
        run: npm test
