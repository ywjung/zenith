global:
  scrape_interval: 60s
  evaluation_interval: 60s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: 'itsm-api'
    static_configs:
      - targets: ['itsm-api:8000']
    metrics_path: '/metrics'
